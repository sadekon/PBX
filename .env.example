# PBX System Environment Variables
# Copy this file to .env and fill in your actual values
# NEVER commit the .env file to version control

# =============================================================================
# DOCKER DEPLOYMENT
# =============================================================================
# When using Docker Compose, these variables are REQUIRED:
# - DB_PASSWORD: PostgreSQL database password (no default)
# - REDIS_PASSWORD: Redis cache password (no default)
#
# To use Docker:
# 1. Copy this file: cp .env.example .env
# 2. Edit .env and set DB_PASSWORD and REDIS_PASSWORD (at minimum)
# 3. Run: docker compose up -d
#
# See INSTALLATION.md "Docker Deployment" section for details.

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
DB_HOST=localhost
DB_PORT=5432
DB_NAME=pbx_system
DB_USER=pbx_user
# REQUIRED for Docker deployment
DB_PASSWORD=YourSecurePassword123!

# =============================================================================
# REDIS CONFIGURATION
# =============================================================================
# Redis is used for caching, session storage, and real-time features
# REQUIRED for Docker deployment
REDIS_PASSWORD=YourRedisPassword456!

# =============================================================================
# EMAIL/SMTP CONFIGURATION
# =============================================================================
SMTP_HOST=smtp.yourserver.com
SMTP_PORT=587
SMTP_SECURITY=starttls
SMTP_AUTH=none
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_CA_FILE=
# TESTING ONLY - diverts every email to this address. Leave empty in production.
SMTP_REDIRECT_TO=

# =============================================================================
# 🆓 OPEN SOURCE INTEGRATIONS (FREE)
# =============================================================================

# Matrix Integration (Free alternative to Slack/Teams Chat)
# No API key needed! Just create a bot account on any Matrix homeserver
MATRIX_BOT_PASSWORD=your-bot-password

# EspoCRM Integration (Free alternative to Salesforce/HubSpot)
# Get API key from EspoCRM: Administration > API Users
ESPOCRM_API_KEY=your-api-key

# Jitsi Meet - No credentials needed for public server (meet.jit.si)
# Only set these if using self-hosted Jitsi with JWT authentication:
# JITSI_APP_ID=your-app-id
# JITSI_APP_SECRET=your-app-secret

# =============================================================================
# PROPRIETARY INTEGRATIONS (Optional - Require Licenses)
# =============================================================================

# Active Directory / OpenLDAP
# Works with both Microsoft AD (proprietary) and OpenLDAP (free)
# Use ldaps:// for secure connection (recommended for production)
# NOTE: These credentials are also used for IP phone LDAP directory access during provisioning
AD_SERVER=ldaps://your-ad-server.com:636
AD_BIND_DN=CN=Administrator,CN=Users,DC=company,DC=com
AD_BIND_PASSWORD=YourActualPassword
# Example for production: Use a dedicated service account instead of Administrator
# AD_BIND_DN=CN=svc-pbx,OU=Service Accounts,DC=company,DC=com
#
# SECURITY TIP: For phone provisioning, consider using a read-only account with minimal
# privileges since the credentials will be stored in phone configuration files

# Zoom Integration (Proprietary - requires Zoom license)
ZOOM_CLIENT_ID=your-zoom-client-id
ZOOM_CLIENT_SECRET=your-zoom-client-secret

# Microsoft Outlook/Teams Integration (Proprietary - requires Microsoft 365)
OUTLOOK_CLIENT_ID=your-outlook-client-id
OUTLOOK_CLIENT_SECRET=your-outlook-client-secret
TEAMS_CLIENT_ID=your-teams-client-id
TEAMS_CLIENT_SECRET=your-teams-client-secret

# =============================================================================
# DEBUGGING / TROUBLESHOOTING
# =============================================================================
# Enable PIN debug logging for voicemail IVR troubleshooting
# WARNING: This logs sensitive PIN data - only enable for testing/debugging!
# Set to 'true' to see detailed PIN collection and verification logs
DEBUG_VM_PIN=false

# =============================================================================
# GRAFANA MONITORING
# =============================================================================
# Grafana admin credentials for the monitoring dashboard (Docker only)
# Change these from the defaults before deploying!
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=changeme

# =============================================================================
# SPEECH RECOGNITION
# =============================================================================
# By default, the system uses Vosk (FREE, offline) - no API key or setup needed!
# Vosk provides offline speech-to-text with no usage limits or costs.
#
# Only configure Google Cloud Speech if you specifically want cloud-based transcription:
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json
