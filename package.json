{
  "name": "pbx-admin-panel",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Warden VoIP PBX Admin Panel",
  "scripts": {
    "dev": "vite admin",
    "build": "tsc --noEmit -p admin/tsconfig.json && vite build admin && mkdir -p admin/dist/js && cp admin/js/*.js admin/dist/js/",
    "preview": "vite preview admin",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "typecheck": "tsc --noEmit -p admin/tsconfig.json",
    "lint": "tsc --noEmit -p admin/tsconfig.json"
  },
  "devDependencies": {
    "@jest/globals": "^30.2.0",
    "@swc/core": "^1.15.43",
    "@swc/jest": "^0.2.39",
    "jest": "^30.2.0",
    "jest-environment-jsdom": "^30.2.0",
    "typescript": "^6.0.3",
    "vite": "^8.1.0"
  },
  "jest": {
    "testEnvironment": "jsdom",
    "testMatch": [
      "**/admin/tests/**/*.test.{js,ts}"
    ],
    "transform": {
      "^.+\\.(js|ts)$": [
        "@swc/jest",
        {
          "jsc": {
            "parser": {
              "syntax": "typescript"
            },
            "target": "es2024"
          },
          "module": {
            "type": "commonjs"
          }
        }
      ]
    },
    "collectCoverageFrom": [
      "admin/js/**/*.{js,ts}",
      "!admin/js/**/*.d.ts",
      "!admin/js/**/*.min.js"
    ],
    "coverageDirectory": "coverage",
    "coverageReporters": [
      "text",
      "text-summary",
      "lcov",
      "html"
    ],
    "coverageThreshold": {
      "global": {
        "branches": 65,
        "functions": 65,
        "lines": 65,
        "statements": 65
      }
    }
  },
  "engines": {
    "node": ">=22"
  }
}
